{
  "name": "staryliskovec-on",
  "version": "1.0.0",
  "private": true,
  "description": "Website for the Starý Lískovec ON coalition (Piráti + Zelení).",
  "scripts": {
    "//": "------- These are shortcuts you run in a terminal with: npm run <name> -------",
    "serve": "http-server . -p 4173 -c-1",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "lh": "lhci autorun"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.48.2",
    "http-server": "^14.1.1"
  }
}
